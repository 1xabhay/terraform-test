[project]
name = "terraform-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "psycopg[binary]>=3.2,<4",
    "redis>=5,<7",
]
